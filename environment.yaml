name: by-workshop
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - git
  - jupyterlab
  - ipykernel
  - pip
  - matplotlib
  - pip:
      - requests
      - uv
      - nvidia-nat
      - nemo-gym
      - openai==2.6.1
      - openpipe-art>=0.6.0
